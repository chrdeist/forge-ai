version: '3.8'

services:
  api:
    build:
      context: ../../../../generated-code/hello-world-feature-3
      dockerfile: ../../projects/hello-world/deployment/feature-3/Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
